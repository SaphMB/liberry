---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: liberries.webapp.my.domain
spec:
  group: webapp.my.domain
  names:
    kind: Liberry
    listKind: LiberryList
    plural: liberries
    singular: liberry
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Liberry is the Schema for the liberries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiberrySpec defines the desired state of Liberry
            properties:
              category:
                description: Foo is an example field of Liberry. Edit liberry_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: LiberryStatus defines the observed state of Liberry
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
